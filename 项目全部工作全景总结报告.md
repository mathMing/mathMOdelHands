# 项目最终工作总结

## 1. 最终冻结方案

当前项目冻结为三模态 DRM-Net 实验流程：文本、音频和视觉特征先统一到最多 50 个时间位置，再通过 Bi-GRU、跨模态注意力、缺失 mask 和动态门控完成融合，联合输出三分类极性和连续情感强度。

最终评估使用三个固定 seed：42、123、777。每个 seed 保存验证集最佳 checkpoint，测试阶段对三 seed 分类概率求平均后采用 Argmax。最终结果为：Accuracy 66.30%，Macro-F1 62.54%，MAE 0.6346，Pearson r 0.6630。

## 2. 三个问题的逻辑链

```text
原始文本/音频/视频
  -> 三模态特征提取
  -> 统一到最多 50 个时间位置并保存有效 mask
  -> 完整模态基线
  -> 训练阶段模拟局部连续缺失
  -> DRM-Net 鲁棒训练
  -> 验证集选择 checkpoint 和策略
  -> 测试集最终评估
  -> 附件3无标签专项预测
  -> 附件4遮挡敏感性解释
```

问题一的结论是建立可复现的特征提取和时间统一映射；问题二的结论是显式缺失增强和 mask 能改善局部缺失下的鲁棒预测；问题三的结论是可以将模型输出变化追溯到模态和时间窗口，但该结果是遮挡敏感性，不是严格因果证明。

## 3. 实验结果

结构消融和损失函数对照分别保存在 `problem2_robust/architecture_ablation_summary.csv` 与 `problem2_robust/loss_ablation_summary.csv`。附件3两套结果各30条，附件4解释结果20条。附件3、附件4均无标签，不计算 Accuracy、F1、MAE 或 Pearson。

## 4. 科学表述边界

最终论文不再声称实现 DTW、ExtraTrees、Wasserstein 元集成、积分梯度或 Attention Rollout；不使用“严格因果贡献”表述；不将未实际运行的缺失比例和性能结论外推为已验证事实。门控权重表示模型内部动态分配，不能直接等价于真实信息量。

## 5. 可追溯交付

- 最终主指标：`problem2_robust/problem2_key_metrics_final_argmax.json`
- 结构消融：`problem2_robust/architecture_ablation_summary.csv`
- 损失对照：`problem2_robust/loss_ablation_summary.csv`
- 验证集分析：`problem2_robust/validation_error_attribution_summary.json`
- 附件3：`problem2_robust/附件3_*.csv`
- 附件4：`problem3_interpretability/附件4_可解释专项测试集预测与解释结果.csv`

原始数据、模型权重和缓存文件保留在本地及服务器，不纳入 GitHub 交付分支。
